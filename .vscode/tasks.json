{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "args": ["-f", "Makefile_kamiak"],
            "options": {"cwd": "${workspaceFolder}/build/Qt/gcc/RHESSys"},
            "group" : {
                "kind" : "build",
                "isDefault": true
            }
        },
        {
            "label": "pwd",
            "type": "shell",
            "command": "pwd",
            "options": {"cwd": "${workspaceFolder}/build/Qt/gcc/RHESSys"}
        }
    ]
}